#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <math.h>
int main() {
    char arr[100];
    printf("Enter String: ");
    scanf("%s", arr);
    int ans = atoi(arr);
    printf("\nInteger: %d", ans);
    int max[100];

    while(ans>0){
     int i=0;
     max[i]=
    }
    return 0;
}